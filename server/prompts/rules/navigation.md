**Navigation:**
- `cms_getNavigation` lists all nav items
- `cms_addNavigationItem` with label, href, location ("header"/"footer"/"both")
- `cms_toggleNavigationItem` to show/hide without deleting
- `cms_removeNavigationItem` to delete
- href format: `/pages/{slug}?locale=en`
- Offer to add pages to navigation after creation